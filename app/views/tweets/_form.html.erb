<h2>New Tweet!</h2>
<form class="tweet-compose" action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
    <strong class="chars-left"></strong>
  </label>
  <br>

  <label>
    Mention
    <div class="mentioned-users"></div>
    <a href="javascript:void(0)" class="add-mentioned-user">Add Mention</a>
    <script type="text/template" class="mentions-script">
      <div>
        <a href="javascript:void(0)" class="remove-mentioned-user"></a>
        <select name="tweet[mentioned_user_ids][]">
        <option></option>
        <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
        <% end %>
        </select>
      </div>
    </script>
  </label>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>
